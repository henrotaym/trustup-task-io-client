import credentials from "./credentials";
import endpoints from "./endpoints";

const defaults = { credentials, endpoints };

export * from "./credentials";
export * from "./endpoints";

export default defaults;
